# omran-ai-builder — الدفتر الحيّ

**تاريخ آخر تحديث: ٦ أغسطس ٢٠٢٦.**
مرجع أعلى: `/tasklet/workspace/home/AGENTS.md` · المشروع: `PROJECT.md` · النشر: `DEPLOY.md` · الفخاخ: `PITFALLS.md`.
خطط المراحل التفصيليّة: `/tasklet/agent/home/audit/{p3,p4,errors,i18n,pricing}/`.

## (أ٠) v426 — **المزوّدون الأربعة: منشور ومُتحقَّق حيًّا** (٦ أغسطس ٢٣:٣٠، بأمر «اعتمد»)
- **القائمة الظاهرة:** Claude · Gemini · OpenAI · Groq — مرتّبة، والخمسة الآخرون خرجوا.
- **خرج نهائيًّا من كلّ المسارات:** OpenRouter · Mistral · DeepSeek · Cohere.
  **Perplexity:** خارج القائمة و«اسأل الكل»، وباقٍ محرّك الأخبار/الأسعار الحيّة + احتياط GPT.
- **الأثر على التكلفة:** «اسأل الكل» ومسابقة نصوص التصاميم كانتا تناديان **٩** مزوّدين بمفاتيح
  المالك في كلّ مرّة — صارت **٤**.
- **بطاقات المفاتيح الخمس مخفيّة لا محذوفة** — `app-06-checkout.js` يقرأ عناصرها؛ الحذف = TypeError.
- **الدفعات:** `13daf72` · `5846b0b` · `762a05c` — ٧/٧ متحقَّقة ببروتوكول git.
  **الإنتاج:** `dpl_CWtNZLnaZokP2xuRfaRr1cdWQXUn` · دخان ١٨/٠ · نقطة الرجوع `dpl_6Ch3u1Xt…`.
- **التحقّق الحيّ على النطاق:** القائمة ٤ · بطاقات مخفيّة ٥/٩ · بركة اسأل الكل ٤ ·
  `AUTO_FALLBACK_ORDER` ٤ · `provs` ٤ · صفر ادّعاء «٩» في النصوص · `stockTickerToggle` سليم · مها سليمة.
- **أسطر اليوم من هذا البند:** ~٨٣ في المصدر (الحزمة مُولَّدة). التفصيل والتراجع:
  `/tasklet/agent/home/audit/providers/DECISION-v426.md`.
- **باقٍ مفتوحًا:** إخراج Perplexity نهائيًّا يحتاج وصل Tavily بمسار الدردشة (عمل يوم) ·
  مفاتيح الخمسة في Vercel لم تُلمس · «9 AI models» باق في ٨ ملفّات لغات (الزرّ مخفيّ، الأثر صفر).

## (أ) المرحلة ٤ شريحة ١+٢ — **مدفوعة ومنشورة ومُتحقَّقة حيًّا** (٦ أغسطس ٢١:١٥، بأمر عمران «الحينه ترفعها»)
- **GitHub:** دفعة `ceba07b3` · ٩ ملفّات · **٩/٩ بصمة blob مطابقة على `main`**.
- **الإنتاج:** `dpl_6Ch3u1XtSHiyqDpJkiWEzPqDzeZp` · READY ١٦ث · دخان **١٨/٠**.
  معاينة قبله: `dpl_4jxL9fVtjn9A3xD9d6AYTXWewNaT` دخان ١٨/٠. **نقطة الرجوع: `dpl_FyDWdCEVL9n5qUmmf8PXMuUg3HR1`.**
- **تحقّق حيّ على `omran-ai-builder.vercel.app`:** الرؤوس الخمس كلّها صحيحة
  (`/`+`/sw.js` = `max-age=0, must-revalidate` · `/js/*`+`/css/*` = `300 + SWR 86400`
  · `/ffmpeg/*` = `31536000, immutable` · `/icons/*` = `86400 + SWR 604800`) ·
  بصمة الحزمة المخدومة = المحلّيّة `10cba084` · الثلاثيّة متّسقة (`index?v=` = `CACHE_NAME` = البصمة)
  · الحارسان موجودان · `stockTickerToggle` سليم (٤ مواضع).
- **نسخة احتياطيّة قبل الدفع:** `backup/main-pre-p4/` — ٧ ملفّات، مُتحقَّقة ٧/٧ بـ`sha256`
  مقابل أساس `main` (و`.nvmrc`/`CONTRIBUTING.md` جديدان لم يكونا موجودين).
- **الشجرة المنشورة صارت ١٩٣ ملفًّا** (١٩١ + `.nvmrc` + `CONTRIBUTING.md`). الإنتاج كان
  مطابقًا لـ`main` في السبعة قبل التركيب — صفر انزياح.
- **`patches/p4-slice1/`** — ٢٤ سطرًا (منطق) + وثيقة. `vercel.json`: ffmpeg سنة `immutable`،
  `js`/`css` ٥ دقائق `stale-while-revalidate`، `icons`/`assets` يوم + أسبوع خلفيّ،
  `index.html`/`sw.js`/`templates-data.js` تبقى طازجة دائمًا (بلا مساس). زائد: `package.json`
  `engines.node=24.x` (+٣) و `.nvmrc=24` (+١) و تصحيح `ci/ci.yml` من ٢٠ إلى ٢٤.
- **`patches/p4-slice2/`** — ٣ سطور. حارسان لـ`btnLangAr`/`btnLangEn` (كانا الاثنين الحقيقيَّين
  غير المحروسين من ٣٢ إسنادًا مدقَّقًا)، وشاشة الدخول تقرأ اللغة الحيّة بدل مفتاح غائب.
- **تحقّق المعاينة `bf1gnz90b` (شريحة ١+٢ معًا):** CI ١٢٥ ملفًّا/١٨ حزمة/١١‑من‑١١ بوّابات
  ملكيّة · دخان **١٨/٠** · DOM = **٢٨٣٩** مطابق تمامًا لأساس المرحلة ٣ · `t` مقفولة (`writable:false`)
  · ١٣/١٣ مفتاحًا وظيفيًّا · فرق الحزمة = الثلاثة أسطر المقصودة بالضبط، لا أكثر.
- **السقف:** كنتُ سأنتظر الغد لأنّ حصيلة ٦ أغسطس بلغت ٦٩١ سطرًا > ٤٠٠. **عمران أمر بالدفع
  الليلة صراحةً**، فنُفِّذ. الحصيلة النهائيّة لليوم: **٧١٩ سطر منطق** (+٤٣ وثيقة) — تجاوزٌ
  مأذون به كتابةً، مسجَّل في `design/DECISIONS.md`.
- **فحص ما قبل الطيران (٦ أغسطس ٢٠:٥٠): صفر انزياح.** سُحبت السبعة أحياءً من `main` وقُورنت
  بـ`sha256`؛ بصمة الحزمة على `main` = `9f4bd748` = أساس الرقعة حرفًا. الجدول الكامل في
  `audit/p4/PHASE-4-PLAN.md`. الاحتساب الأقصى للدفعة: ٢٨ سطر منطق + ٤٣ سطر وثيقة = **٧١**.
- **⚠️ خطأ تسمية أُصلح:** `backup/main-post-p3/` كان في الحقيقة لقطة **قبل** الدفع (مطابقة
  بايت-ببايت لـ`main-pre-p3`, ٤٨٠٥ سطرًا). الاسترجاع منها كان سيمحو المرحلة ٣. اللقطة الصحيحة
  الآن: **`backup/main-post-p3-TRUE/`** (٨ ملفّات، `index.html` ٤١٦ سطرًا، `f2225464`).

## محاسبة السطور — ٦ أغسطس
- **إجماليّ اليوم النهائيّ: ٧١٩** = ٦٩١ + المرحلة ٤ (٢٨ سطر منطق). تجاوز السقف بأمر عمران المكتوب.
- **تفصيل الـ٦٩١** (دفعة ١٨ = ٦٧١ سطرًا حقيقيًّا مُعاد احتسابها بشمول أدوات الوكيل +
  منطق المرحلة ٣ الجديد الصافي = ٢٠ سطرًا؛ النقل الحرفيّ المُثبَت بـ`sha256` لبقيّة المرحلة ٣
  = صفر سطر محتسَب — انظر قاعدة الاحتساب في `PITFALLS.md` §٤).
- **البوّابة ② مُغلقة رسميًّا ٦ أغسطس:** **١٩/١٩** جذرًا (① `sendPrompt` ×١١ · ② `I18N` ×٢ ·
  ③ `t is not a function` ×٦) مُصلَحة ومُتحقَّق منها حيًّا في الإنتاج. سجل KV نُظِّف انتقائيًّا
  من أثر جلسات الوكيل: ٢١→١٩ حادثة (تفاصيل الطرح في `PITFALLS.md` §١).

## (ب) بانتظار كلمة عمران — لا استنتاج، لا تنفيذ
| البند | الخيارات المطروحة | المرجع |
|---|---|---|
| تسعير Plus/Pro | (أ) رفع الخادم إلى ١٠$/٢٠$ ليطابق البطاقة (٣٦ سطرًا) — رأي الوكيل المفضَّل، أو (ب) تنزيل البطاقة إلى ٥$/١٥$ ليطابق الخادم/الدفع الفعليّ (٣٤ سطرًا). **الرقعتان مبنيّتان جاهزتان** في `patches/pricing/A/` و`B/`. اكتشاف حرِج: شاشة الدفع والخصم الفعليّ متّفقان على ٥$/١٥$ — **البطاقة هي الشاذّة**، فلا أحد ظُلم ماليًّا (الخصم أقلّ من المعلن دائمًا). | `audit/pricing/PRICING-FIX.md` §١-٢ |
| حصّة Plus الشهريّة | التناقض: مجّانيّ = ٢٠ رسالة/يوم ≈ ٦٠٠/شهر، بينما وعد Plus = ٣٠٠/شهر فقط — **من يدفع يحصل على نصف ما يحصل عليه المجّانيّ**. مقترَح الوكيل: Plus = ٦٠ رسالة/يوم (بعدّاد يوميّ قائم، صفر بنية جديدة) بدل ٣٠٠/شهر. | نفس المرجع §٣ |
| `user.plan` لا يُقرأ | `api/_lib/_usage.js` لديه حدّ ثابت `DAILY_LIMIT=20` بلا أي فرع لـ`plan` — **من يدفع ويصبح Pro يبقى محدودًا بـ٢٠ رسالة/يوم كالمجّانيّ**. رقعة `limitFor(username)` جاهزة (٢٧ سطرًا، ٧ حالات `plan` مختبَرة). | نفس المرجع §٤ |
| لا Stripe webhook | الترقية معلَّقة كلّها على رجوع المستخدم بـ`session_id` — إغلاق التبويب بعد الدفع = **دفع بلا ترقية**. أكبر بند وأخطره، ≈٧٠ سطرًا، يستحقّ يومًا كاملًا. | نفس المرجع §٦ |
| النقاط الشهريّة/الانتهاء | «٦٠ نقطة/شهر» و«٢٠٠ نقطة/شهر» معروضتان بصفر كود يمنحهما؛ `planUpdatedAt` يُكتب ولا يُقرأ ⇒ من يدفع مرّة يبقى مشتركًا للأبد بلا تجديد أو انتهاء. ≈٣٥ سطرًا. | نفس المرجع §٥ |
| `btnAuthLangAr`/`En` | إعادتهما فعليًّا داخل شاشة الدخول (زرّان في `js/partials-core.js`) — تغيير واجهة. | `PITFALLS.md` §٣، `audit/p4/PHASE-4-PLAN.md` |
| ٧ لغات فارغة تقريبًا (es/ru/zh/tr/id/fil/ml، ~٣٠/٨٨٢ مفتاحًا) | (أ) ترجمة تدريجيّة بالأهمّ أوّلًا، (ب) ترجمة آليّة دفعة واحدة بمراجعة، (ج) إخفاء السبع من قائمة اللغات. الحجم ≈٦٠٠٠ سطر (×١٥ السقف) — يحتاج قرارًا قبل أي تنفيذ. | `audit/i18n/FINDING.md`، `PROJECT.md` §٦ |
| ffmpeg من CDN | يوفّر ٣٠٫٦ م.ب من النشر لكنه يحوّل ميزة الفيديو إلى رهينة مضيف خارجيّ (CORS + توفّر)؛ الملفّ مؤجَّل التحميل أصلًا ويُخزَّن سنة كاملة بعد شريحة ١. **مؤجَّل حتى يأمر عمران.** | `audit/p4/PHASE-4-PLAN.md` |
| تصغير الحزمة (esbuild) | الشبكة تنقل ٣٣٧ك.ب مضغوطة أصلًا؛ الربح في زمن التحليل لا البايتات، والخطر تصغير حزمة بلا اختبارات وحدة. **بعد الاختبارات، لا قبلها.** | نفس المرجع |
| قاعدة "sha256 نقل = صفر سطر" | مُطبَّقة فعليًّا (المرحلة ٣ = ٢٠/٤٤٠٦ سطرًا محتسَبة) بأمر شفهيّ («ارفع») — **تحتاج تأكيدًا رسميًّا مكتوبًا** لتصبح قاعدة دائمة لا استثناءً حالة بحالة. | `DEPLOY.md` §٨، `PITFALLS.md` §٤ |
| حارس عمران اليوميّ (watchdog) | `cti_tjp2bzbgzt5yxt39ef0n` — فحص ٠٧:٠٠ دبي — **معطَّل حاليًّا**. إعادة تفعيله تحتاج أمرًا. | — |
| كتابة شخصيّة عمران في التطبيق | لم تُنفَّذ. تحتاج توجيهًا عن المحتوى والنطاق. | — |
| ٣٠ ملفًّا تاريخيًّا في GitHub + مشاركة عامّة `ace2442e1968` | حذف أو إبقاء — قرار تنظيف لا تقنيّ. | `PITFALLS.md` §٢ |
| مصير التطبيق الفوريّ `omran-command` | غير محسوم. | — |
| توسيع مدى سحب `chatcol` | تحسين واجهة مطلوب تحديد نطاقه. | — |

## (ج) أبواب مقفلة تعيق أعطالًا حقيقيّة قائمة اليوم

> **قرار عمران — ٦ أغسطس ٢٠٢٦ ٢١:٤١:** **باب المال مقفل حتّى تصدر الرخصة التجاريّة.**
> الأعطال المالية الخمسة (webhook سترايب · `user.plan` لا يُقرأ · `create-checkout-session` ٥٠٠ ·
> النقاط بلا انتهاء · Veo 3) **معروفة ومقيسة ومجمَّدة** — لا تُفتح، ولا يُسأل عنها مرّة أخرى
> حتّى يكتب عمران أنّ الرخصة صدرت. لا تشخيص، لا رقعة، لا «تحضير احتياطًا».

هذه ليست ميزات مؤجَّلة — هي **أعطال فعليّة يعرفها المستخدمون الآن** ولا يمكن إصلاحها لأن
الإصلاح يعبر بابًا من الخمسة المقفلة في `AGENTS.md` (المال/بيانات المستخدمين/حذف/إنفاق/أسرار):
- **٤ متغيّرات بيئة ماليّة غير مضبوطة في Vercel** — تمنع تشغيل أجزاء من مسار الدفع.
- **`create-checkout-session` يرجع ٥٠٠`** بسبب مصادقة تُفحَص بعد إرسال الاستجابة (ترتيب خاطئ
  في الكود) — عطب حقيقيّ في مسار مال، الإصلاح يحتاج لمس ملفّ دفع فيتطلّب إذنًا صريحًا أولًا.
- **Veo 3 (توليد الفيديو) ميتة تمامًا لغير المالك، بلا استرداد نقاط** — مستخدم غير المالك
  يخسر نقاطه على ميزة لا تعمل له.

## (د) الدَّين التقنيّ المتبقّي (لا قرار مالك مطلوب، فقط وقت وسقف)
| البند | الحجم المقيس | ملاحظة |
|---|---|---|
| ~~شريحة ٨ من المرحلة ٣~~ | **مُغلَقة: لا تُنقل** | قياس حيّ ٦ أغسطس: الباقي داخليًّا ٣ كتل / ٣٣ سطرًا، وكلّها **يجب** أن تبقى داخليّة (بذرة `__swallow` قبل كلّ كود · `ld+json` بيانات فهرسة · سطر `waCollapsed` قبل الرسم). المرحلة ٣ منتهية عند ٤١٦ سطرًا (−٩١٪). التفصيل: `audit/p3/PHASE-3-MAP.md` |
| `readBody` — جسم مشوَّه بـ`application/json` يرجع ٥٠٠ لا ٤٠٠ | **مقيس ٦ أغسطس: ٤٨ ملفًّا فيها `JSON.parse(body`، منها ٣ محميّة فقط ⇒ ٤٧ موضعًا مكشوفًا** (≈٥٥ سطرًا: مساعد `_body.js` ٨ + سطر لكلّ موضع) | `api/_lib/agent.js` نفسه محميّ (`safeParse`). الخطر ليس في السطور بل في **لمس ٤٨ ملفّ واجهة برمجيّة بلا أي اختبار وحدة**. الطريق الآمن: يوم مخصَّص + دخان لكلّ مسار، أو ٦ مسارات الأكثر استخدامًا فقط (≈١٠ أسطر) |
| ٧٢ موضع «نصف قطر» في الحزمة | **مُدقَّقة فرديًّا ٦ أغسطس** | ٢٩ تبديل آمن بصفر تغيير بصريّ · ٨ أشكال هندسيّة صحيحة (`50%`/`999px`) · **٣٥ قرار بصريّ لعمران**. الاستنتاج: أكثر قيمتين استخدامًا (`12px`×١٤ و`8px`×١٢) **غائبتان عن سلّم الرموز** — الرموز ناقصة لا الكود. التوصية: لا تُنفَّذ وحدها، تُدمج مع أوّل مرور تصميم. التفصيل: `audit/design/RADIUS-AUDIT.md` |
| Preview-per-PR على Vercel | معلَّق | يحتاج `link` غير `null` — أي ربط المشروع بـGitHub، وهذا قرار بنية نشر لا سطر كود |

## قاعدتان تشغيليّتان دائمتان
1. **حدِّث هذا الملفّ فور كل دفع فعليّ** (سواء إلى معاينة أو إنتاج) — لا في نهاية اليوم فقط.
2. خطط المراحل التفصيليّة (الكاملة، غير الملخَّصة) تعيش في `/tasklet/agent/home/audit/`؛
   هذا الملفّ ملخَّص حيّ يشير إليها، لا يستبدلها.

## v441 — قتل الذهبيّ نهائيًّا (٧ أغسطس ٢٠٢٦) ✅ منشور
- أمر عمران: «احذف لون الذهبي من كل مكان، يبقى في الشعار وحده».
- commit `c6aa0ff5` · ١٨ ملفًّا · ٦٢ سطرًا · rollback `dpl_8FCiDdrVfCVnvxVvefRPniKvFM4s`.
- المصدر الحقيقيّ للذهبيّ كان اثنين: `--accent:#d4af37` في `tokens.css` **و** `APP_THEMES.gold` في `js/themes.js` (يكتب `--accent` سطريًّا على `<html>` فيعلو على CSS).
- البديل المحايد: `--accent:#6b7280` · `--accent-rgb:107,114,128` · `--accent-light/2:#9ca3af` · `#8a6d1f`→`#4b5563`.
- كلّ `rgba(212,175,55,x)` و`rgba(232,199,102,x)` في CSS → `rgba(var(--accent-tint-rgb),x)` (أبيض في الداكن · أسود في الفاتح).
- خطأ theme `purple` كان أوّل قيمته `#d4af37` → صُحّح إلى `#7c5cff`.
- قياس حيّ على الإنتاج: عناصر ذهبيّة **١** فقط = `.cc` (فقاعة لوحة ألوان يختارها المستخدم لتصاميمه) — محميّة عمدًا مع سطر `DESIGN_POSTER_RULE`.
- الشعار ومها صورتان — ذهبهما محفور في الملفّ لا في الكود.
- ⚠️ فخّ مُثبت: تعديل `js/app.bundle.js` مباشرةً يوقفه حارس ما قبل النشر («متأخّر عن أجزائه») — عدّل `js/app-NN-*.js` ثمّ `npm run bundle`. البصمة الناتجة طابقت التعديل المباشر بالحرف (`177e12c0`).

## v439 + v440 — تهذيب الذهبيّ (٧ أغسطس ٢٠٢٦) ✅ منشور
- v439 `125b9922`: الذهبيّ نُزع من النصّ والخلفيّات والتوهّج · بقي على الحدود والفواصل. ١٤٥ سطرًا.
- v440 `f90ce17a`: إصلاح — `--accent-ink`/`--accent-on-surface` ثابتان (#eef0f6 داكن · #14161a فاتح) بدل `var(--text)`. ٣ أسطر.
- قياس حيّ (داكن): ذهبيّ مرئيّ = نصّ ٠ · خلفيّة ٠ · توهّج ٠ · حدود ١١ · تباين منخفض ٠.
- الشعار (brandImg) والشبكة ثلاثيّة الأبعاد (JS) بقيا ذهبيّين — لم يُلمسا.
- **عيب مفتوح:** الوضع الفاتح + خلفيّة ثلاثيّة الأبعاد داكنة ⇒ نصّ داكن فوق أسود (١٠ عناصر).
  كان مخفيًّا قبل الرقعة لأنّ النصّ كان ذهبيًّا. ينتظر قرار عمران.
- rollback: `dpl_HcZ5FttxjfYHkg2TvsGieAZwqjeC` (v439) · `dpl_FUwNhchbKFMhoP4TFM13r2WjgbhU` (قبل v439).
- نسخة احتياطيّة: `backup/main-pre-goldtidy/`. سطر اليوم: ٣٠٧/٤٠٠ · دفعتان (تجاوز قاعدة الدفعة الواحدة بأمر «انشر» + إصلاح).
