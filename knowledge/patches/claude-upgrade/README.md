# ترقية كلود — sonnet-4 (مايو ٢٠٢٥) ← sonnet-5

بأمر عمران «رقّي كلود» — ٧ أغسطس ٢٠٢٦.
**مبنيّة ومُتحقَّقة — غير مدفوعة.** السبب: سقف اليوم (٨٠٩/٤٠٠).

## النطاق: ٧ أسطر · ٤ ملفّات · الخادم فقط
- `api/_lib/claude.js` ×٢ — الافتراضي + سطر إعادة التوجيه
- `api/_lib/agent.js`  ×٣ — افتراضي الوكيل + ترتيب تفضيل `resolveModel`
- `api/edu.js`         ×١ — `MODEL`
- `api/_lib/stocks.js` ×٢ — تحليل الأسهم/الذهب

## الحيلة المهمّة
سطر إعادة التوجيه في `claude.js` يحوّل `claude-sonnet-4-20250514` ← `claude-sonnet-5`
عند وصول الطلب. لذا **كلّ متصفّح ثبّت النسخة القديمة في `localStorage` يُرقّى تلقائيًّا**
بلا لمس الحزمة ولا `app-06-checkout.js` — صفر خطر `missing_files`.

## شبكة الأمان (موجودة أصلًا، لم تُبنَ)
`claude.js` عند 404 `model_not_found` يسأل `/v1/models` الحيّة ويختار أحدث sonnet.
فلو رفض المفتاح الاسم الجديد، يعود تلقائيًّا لنموذج شغّال. لا انقطاع.

## متبقٍّ معروف (لم يُلمس)
- سلاسل العميل في `app.bundle.js` / `app-06-checkout.js` / `app-15` / `app-16` ما زالت القديمة —
  مغطّاة بإعادة التوجيه أعلاه، **إلّا** المستخدم الذي أدخل مفتاحه الخاصّ (نداء مباشر بلا خادم).
- `api/telegram.js` يستعمل `claude-sonnet-5` أصلًا.
- `points.js` يُبقي `claude-opus-5` للبريميوم — لم يُلمس.

## التحقّق المنفَّذ
`node --check` على الملفّات الأربعة ✅ · فرق مقيس ٧ أسطر ✅ · صفر تغيير خارج سلاسل النموذج ✅
