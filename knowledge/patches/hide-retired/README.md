# رقعة: إخفاء أزرار الميزات المتقاعدة
الهدف: الواجهة تكذب — ٣ أزرار تفتح نوافذ ثمّ يردّ الخادم 410.

- الملفّ: `index.html` وحده.
- التغيير: إضافة `hidden style="display:none"` إلى `btnDocs` · `btnGov` · `btnCV`.
- ٣ أسطر مُعدَّلة · صفر سطر منطق جديد · ٤١٦ سطرًا قبل = ٤١٦ بعد.
- `index.html.before` = نسخة الإنتاج الحيّة كما سُحبت (مرجع تحقّق).
- التراجع: حذف `hidden style="display:none"` من الثلاثة.
- **لم تُنشر.** تنتظر «انشر» من عمران (تصحيح P4: لا نشر بلا طلب).
