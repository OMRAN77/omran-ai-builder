# المرحلة ٣ — شريحة ١: استخراج CSS من index.html
**جاهزة، معاينة فقط. لم تُدفع إلى `main` (سقف اليوم مستهلك).**

- `index.html`: ٤٨٠٦ → **٣٩٣٥** سطرًا (−٨٧١)
- ملفّان جديدان: `css/tokens.css` (٦٧٦) + `css/redesign.css` (١٩٢)
- بديلهما في `<head>` السطر ٨٠–٨١: وسمَا `<link ... ?v=418>` بنفس ترتيب التتالي
- لا معرّف style مستخدَم في JS ⇒ لا كسر مراجع
- `sw.js` بلا قائمة أصول ⇒ لا تسجيل مطلوب

## التحقّق (حيّ)
- CI ١١/١١ · دخان ١٨/٠ · حزمة ١٨ جزءًا بصمة `9f4bd748`
- معاينة: https://omran-ai-builder-o9cf88zgx-omran4.vercel.app
- مقارنة ١٤٠ عنصرًا (موضع/حجم + ١٨ خصيصة) إنتاج ↔ معاينة:
  **٠ اختلاف في الموضع أو الحجم.**
  الفرق الوحيد `fontFamily` في ٧٣ عنصرًا — سببه `html[dir="ltr"] body{...}`
  (المعاينة أصل جديد ⇒ لغة إنجليزيّة افتراضيّة)، لا علاقة له بالاستخراج.

## للترقية غدًا
bun scripts/deploy-files.ts production "..." index.html css/tokens.css css/redesign.css
ثمّ gh-push.ts للملفّات الثلاثة.
**ملاحظة:** علامة `v411` انتقلت من index.html إلى `css/redesign.css` — تحقّق هناك.
