# مزامنة GitHub ← الإنتاج — 2026-08-05

**الأمر:** «ثبتها كلها في الجيت هوب» (عمران، 19:51 GMT+4)
**المستودع:** `OMRAN77/omran-ai-builder` · `main` · **عامّ (public)**
**مصدر الحقيقة:** نشر الإنتاج `dpl_DJBY1E7mZYh9i377j5rFpvcwz9R2` (v414) — 164 ملفًا مسحوبة طازجة.

## قبل
- GitHub `index.html`: 4400 سطر · 430KB · sha256 `80d4cd61` (حدود v412)
- الإنتاج `index.html`: 4541 سطر · 451KB · sha256 `9eb11b49` (v414)
- المقارنة الدقيقة (git blob sha1 لكل ملف): 153 متطابق · 9 مختلف · 1 جديد

## الفحص الأمني قبل الدفع (المستودع عامّ)
- لا مفاتيح حقيقية: صفر تطابق لأنماط Stripe live/test، OpenAI `sk-`، Google `AIza`، GitHub PAT، Slack، PRIVATE KEY، Bearer حرفي.
- **مستبعَد من الدفع:** `.vercel/project.json` — يحمل `team_…` و`prj_…`. لا يُنشر على مستودع عامّ.
- قيم احتياطية داخل الكود: 17، كلها غير سرّية (`OWNER_USERNAME='omran'`, `SITE_URL`, أسماء مناطق/موديلات).
- **نتيجة مهمّة:** `MONITOR_KEY` و`AUTH_SECRET` لم تعودا مضمّنتين في الكود — `api/_lib/_secrets.js` يفرضهما من البيئة عبر `requireSecret()`. بند «تدوير MONITOR_KEY» في القائمة **مُنجَز فعلًا في الإنتاج**.

## الدفع (3 عمليات، كل واحدة مُتحقَّق منها بالبصمة)
| # | الملفات | الحجم | commit |
|---|---|---|---|
| 0 | `index.html`, `p.html`, `sw.js`, `.tasklet-source-revision`, `.vercelignore` | 449KB | `3842880` |
| 1 | `js/app.bundle.js` | 1098KB | `3c77517` |
| 2 | `icons/favicon-32-v2.png`, `icon-192-v2.png`, `icon-512-v2.png`, `apple-touch-icon-v2.png` | 398KB | `79beb3c` |

## بعد (إعادة فحص كامل الشجرة)
- **162 من 163 ملفًا متطابقة تمامًا** بالبصمة (git blob sha1).
- الاستثناء الوحيد: `.tasklet-source-revision` — ملف علامة من المنصّة، محتواه UUID عشوائي يتغيّر بكل نسخ. ليس كودًا للموقع. متروك كما هو.
- 30 ملفًا تاريخيًا موجودة في GitHub وليست في الإنتاج (فيديوهات، `maha-icon.png`, `source-code.zip`, `specs/`). **لم تُحذف** — لا حذف بلا أمر.

## الأثر على الحدّ اليومي
**صفر سطر.** لم يُلمس كود الموقع الحيّ ولا نُشر أي شيء على Vercel. عملية أرشفة فقط.

## سكربتات
- `/tasklet/agent/home/scripts/gh-gap-check.ts` — يقيس الفجوة.
- `/tasklet/agent/home/scripts/gh-tree.ts` — يسرد شجرة GitHub ببصماتها → `audit/github-gap/gh-tree.json`.
- `/tasklet/agent/home/scripts/gh-sync-push.ts` — يدفع بالدفعات ويتحقّق بعد كل دفعة (`bun gh-sync-push.ts <رقم الدفعة>`).
