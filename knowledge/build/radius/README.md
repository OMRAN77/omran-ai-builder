# v419 — توحيد الاستدارة (منشورة ومُتحقَّقة)
مُنشورة: ٦ أغسطس ٢٠٢٦ — `dpl_6tk7TctVrYYY…` · GitHub `0751ddc` · رجوع `dpl_9rYtKmmk…`

## السلّم
`--r-1:6px` شارة · `--r-2:10px` زرّ/حقل · `--r-3:14px` بطاقة · `--r-4:20px` نافذة · `--r-pill:999px`

## الأرقام
- ٢٦٨ موضعًا وُحِّدت (١١٠ في CSS + ١٥٨ في سمات `style`) — من ٢٦ قيمة مبعثرة إلى ٥ رموز.
- التوزيع: r-2 ١٥١ · r-3 ٥٦ · r-4 ٣٣ · r-1 ٢١ · pill ١٠.
- ٢٧٣ سطرًا متغيّرًا (٢٦٧ بديل + ٦ تعريف السلّم).

## متروك بقصد
- `50%` ×٣٢ (الدوائر — موحّدة أصلًا) و`0` ×٥.
- ٤ مواضع جوال: `header h1 .brand` ٩px و`dialog` 0 — في `@media(max-width:860px)` و`html.mobile-ui`.
- ٤ مواضع مال (باب مقفول، تنتظر أمرًا مكتوبًا): `#premiumPointsChip` ١٠px · `#btnPremiumToggle` ١٢px · زرّ Stripe · زرّ PayPal.

## تحقّق ساكن (كله أخضر)
- ٠ سطر جوال متغيّر · ٠ سطر مال متغيّر.
- `!important` ٨ قبل و٨ بعد · ٠ خطأ نمطيّ (`var(var(`, `--r-0`, `--r-5`).
- عدد سمات `style="` ثابت ١٠٤٢ · عدد الوسوم ثابت → الترميز سليم.
- ٧ مواضع داخل `<script>` فُحصت واحدًا واحدًا: كلها تُحقن في المستند نفسه (`:root` متاح) — لا `srcdoc` ولا نافذة طباعة تعتمد عليها.
- ذيل فقاعة الدردشة `14px 14px 4px 14px` → `var(--r-3) var(--r-3) var(--r-1) var(--r-3)`.

## غير مُتحقَّق
الشكل البصريّ الحقيقي — يحتاج عينًا على الصفحة الحيّة بعد النشر.

## الخطوة التالية
`bun deploy-files.ts production "…"` ثم لقطة شاشة للتحقّق البصريّ، ثم دفع GitHub.
رجوع: `/tasklet/agent/home/backup/rollback-points/pre-radius/index.html`
