<!--
    Copyright 2019 Google Inc. All Rights Reserved.

     Licensed under the Apache License, Version 2.0 (the "License");
     you may not use this file except in compliance with the License.
     You may obtain a copy of the License at

         http://www.apache.org/licenses/LICENSE-2.0

     Unless required by applicable law or agreed to in writing, software
     distributed under the License is distributed on an "AS IS" BASIS,
     WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
     See the License for the specific language governing permissions and
     limitations under the License.
-->

<!-- The "package" attribute is rewritten by the Gradle build with the value of applicationId.
     It is still required here, as it is used to derive paths, for instance when referring
     to an Activity by ".MyActivity" instead of the full name. If more Activities are added to the
     application, the package attribute will need to reflect the correct path in order to use
     the abbreviated format. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.omran.aibuilder.twa">


        <uses-permission android:name="android.permission.INTERNET"/>

        <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>


    <!-- v-maha-webview-mic: بلا صلاحية أندرويد هذي، getUserMedia(audio) يفشل فورًا وبشكل دائم
         داخل WebViewFallbackActivity (الأجهزة بلا متصفّح يدعم TWA — أغلب أجهزة هواوي) بغضّ النظر
         عن أي إصلاح جافاسكربت، لأنّ صلاحية "خطِرة" (dangerous) كهذي تحتاج إعلانًا هنا قبل أن
         يقدر أي كود (حتى داخل WebChromeClient) يمنحها وقت التشغيل. إعلانها وحده غير كافٍ —
         يبقى الطلب الفعليّ وقت التشغيل (ActivityCompat.requestPermissions) ومنح WebView نفسه
         (WebChromeClient.onPermissionRequest) غير مؤكَّدين هنا لعدم توفّر مصدر
         androidbrowserhelper:2.6.2 للتحقّق أو بيئة بناء/جهاز لاختبار حيّ (راجع knowledge/DECISIONS.md). -->

        <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- v-maha-webview-mic-4: صلاحيّة RECORD_AUDIO وحدها لا تكفي لالتقاط صوت فعليّ داخل
         WebView أندرويد — مصادر متعدّدة (Chromium/WebRTC، ومكتبات WebView أخرى كـCapacitor)
         توثّق أنّ فتح مسار التقاط الصوت (AudioRecord/AudioManager) داخل WebView يحتاج أيضًا
         android.permission.MODIFY_AUDIO_SETTINGS معلَنة في البيان (صلاحيّة "عاديّة" تُمنح
         تلقائيًّا عند التثبيت بلا طلب وقت تشغيل) — غيابها يفشل التقاط الصوت على مستوى النظام
         بخطأ يصل الجافاسكربت كـNotReadableError/TrackStartError (بالضبط رسالة «المايك مشغول
         ببرنامج ثاني»)، رغم أنّ onPermissionRequest منح RESOURCE_AUDIO_CAPTURE بنجاح
         (v-maha-webview-mic-2) لأنّ الفشل هنا في طبقة أخرى غير طبقة صلاحية WebView. -->

        <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>

    <!-- v-reply-export: منزّل النظام (DownloadManager) في MahaWebViewFallbackActivity يحفظ في
         «التنزيلات» العامّة؛ أندرويد ٩ وما قبله يحتاج هذه الصلاحيّة لذلك، والأحدث لا يحتاجها. -->

        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>



    

    

    <application
        android:name="Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        
        android:manageSpaceActivity="com.google.androidbrowserhelper.trusted.ManageDataLauncherActivity"
        
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Translucent.NoTitleBar">

        <meta-data
            android:name="asset_statements"
            android:resource="@string/assetStatements" />

        

        <meta-data
            android:name="twa_generator"
            android:value="@string/generatorApp" />

        

        

        
            <activity android:name="com.google.androidbrowserhelper.trusted.ManageDataLauncherActivity"
                android:exported="false"
                android:enabled="true"
                android:excludeFromRecents="true">
                <meta-data
                    android:name="android.support.customtabs.trusted.MANAGE_SPACE_URL"
                    android:value="@string/launchUrl" />
                <intent-filter>
                    <action android:name="android.intent.action.APPLICATION_PREFERENCES" />
                    <category android:name="android.intent.category.DEFAULT" />
                </intent-filter>
            </activity>
        

        <activity android:name="LauncherActivity"
            android:alwaysRetainTaskState="true"
            android:label="@string/launcherName"
            android:exported="true">
            <meta-data android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="@string/launchUrl" />

            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/colorPrimary" />

            

            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR_DARK"
                android:resource="@color/colorPrimaryDark" />

            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/navigationColor" />

            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR_DARK"
                android:resource="@color/navigationColorDark" />

            <meta-data
                android:name="androix.browser.trusted.NAVIGATION_BAR_DIVIDER_COLOR"
                android:resource="@color/navigationDividerColor" />

            <meta-data
                android:name="androix.browser.trusted.NAVIGATION_BAR_DIVIDER_COLOR_DARK"
                android:resource="@color/navigationDividerColorDark" />

            <meta-data android:name="android.support.customtabs.trusted.SPLASH_IMAGE_DRAWABLE"
                android:resource="@drawable/splash"/>

            <meta-data android:name="android.support.customtabs.trusted.SPLASH_SCREEN_BACKGROUND_COLOR"
                android:resource="@color/backgroundColor"/>

            <meta-data android:name="android.support.customtabs.trusted.SPLASH_SCREEN_FADE_OUT_DURATION"
                android:value="@integer/splashScreenFadeOutDuration"/>

            <meta-data android:name="android.support.customtabs.trusted.FILE_PROVIDER_AUTHORITY"
                android:value="@string/providerAuthority"/>

            <meta-data android:name="android.app.shortcuts" android:resource="@xml/shortcuts" />

            <meta-data android:name="android.support.customtabs.trusted.FALLBACK_STRATEGY"
                android:value="@string/fallbackType" />

            

            

            <meta-data android:name="android.support.customtabs.trusted.SCREEN_ORIENTATION"
                android:value="@string/orientation"/>

            

            

            

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"
                    android:host="@string/hostName"
                
                />
            </intent-filter>

            

            
        </activity>

        <activity android:name="com.google.androidbrowserhelper.trusted.FocusActivity" />

        <!-- v-fold-config + v-webview-opaque: انظر النشاط التالي — غير مستعمل فعليًّا
             (getFallbackStrategy يوجّه إلى MahaWebViewFallbackActivity) لكنّه يبقى مطابقًا
             حتّى لا يعود العطب إن تغيّر التوجيه. -->
        <activity android:name="com.google.androidbrowserhelper.trusted.WebViewFallbackActivity"
            android:theme="@style/OmranWebViewTheme"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|keyboardHidden|uiMode" />

        <!-- v-maha-webview-mic: LauncherActivity.getFallbackStrategy() يوجّه هنا بدل الكلاس أعلاه
             (يبقى مُعلَنًا بلا ضرر — غير مُستعمَل فعليًّا الآن) لأنّه وحده يمنح صلاحية المايك
             وقت التشغيل. نفس android:configChanges الأصليّ. -->
        <!-- v-webview-opaque: سمة معتمة بدل Theme.Translucent.NoTitleBar الموروثة من
             <application> — نافذة شفّافة لا تضمن سطحًا معتمًا، فيخرج ما لم يرسمه WebView
             ضجيجًا ملوّنًا ومستطيلات سوداء (راجع res/values/styles.xml).

             v-fold-config (جهاز المالك HUAWEI Mate X7 **قابل للطيّ**): configChanges كان
             "orientation|screenSize" وحدهما. طيّ الجهاز أو فتحه يغيّر screenLayout و
             smallestScreenSize، وهما غير معلنين — فيهدم أندرويد النشاط ويعيد إنشاءه، أي
             WebView جديد و**إعادة تحميل الصفحة كاملة**: ومضة الشاشة كلّها وشاشة نصف مرسومة
             عند كلّ طيّ/فتح. وكذلك density (تغيير حجم العرض) وuiMode (الوضع الداكن)
             وkeyboardHidden. إعلانها يُبقي النشاط حيًّا ويسلّم التغيير إلى
             onConfigurationChanged، وWebView يعيد التخطيط وحده بلا إعادة تحميل. -->
        <activity android:name=".MahaWebViewFallbackActivity"
            android:theme="@style/OmranWebViewTheme"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|keyboardHidden|uiMode" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="@string/providerAuthority"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

        <service
            android:name=".DelegationService"
            android:enabled="@bool/enableNotification"
            android:exported="@bool/enableNotification">

            
                <meta-data
                    android:name="android.support.customtabs.trusted.SMALL_ICON"
                    android:resource="@drawable/ic_notification_icon" />
            

            <intent-filter>
                <action android:name="android.support.customtabs.trusted.TRUSTED_WEB_ACTIVITY_SERVICE"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </service>

        
            <activity android:name="com.google.androidbrowserhelper.trusted.NotificationPermissionRequestActivity" />
        

        
    </application>
</manifest>
