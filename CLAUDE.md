# CLAUDE.md — دليل Claude Code في مستودع «عمران AI»

يُقرأ تلقائيًّا في كلّ جلسة Claude Code، سواء في جلسة المالك أو في GitHub Actions
(`.github/workflows/omran-agent.yml`). المالك: **عمران** (OMRAN77)، يكتب بالعربيّة الخليجيّة
ويريد ردودًا قصيرة مباشرة. اللغة الأساسيّة للكود والتعليقات والالتزامات: العربيّة الفصحى.

## البنية في سطور
- **الواجهة:** `index.html` + `js/app-NN-*.js` (٤١ جزءًا) تُبنى إلى `js/app.bundle.js` بـ`npm run bundle`
  الذي يختم `index.html` و`sw.js` ببصمة المحتوى. **لا تحرّر الحزمة يدويًّا أبدًا.**
  `js/partials-*.js` تُحمَّل منفصلة من `index.html` بوسم `?v=NNN` — ارفع الرقم عند تغييرها.
- **الخادم:** `api/*.js` موجّهات Vercel Serverless (حدّ ١٢ دالّة) توزّع الإجراءات على `api/_lib/*.js`.
  المحادثة الرئيسيّة `api/_lib/chat.js`، وكيل التطبيق `api/_lib/agent.js`، التفويض إلى Actions
  `api/_lib/agent-delegate.js`، الخزنة `api/_lib/secrets.js`، التخزين KV عبر `api/_lib/kv.js`.
- **النماذج:** كلود مباشرة من Anthropic بمفتاح `ANTHROPIC_API_KEY`، أو عبر OpenRouter بمفتاح
  `OPENROUTER_API_KEY`؛ الباقي (Gemini، OpenAI، Groq…) كلّ مزوّد بملفّه. المستخدم يختار نموذج كلود من
  الإعدادات (`CLAUDE_MODELS` في chat.js).
- **الاختبارات:** `tests/*.test.cjs` بـ`node:test`، مسجّلة واحدًا واحدًا في السكربت `test` داخل `package.json`.
- **المعرفة:** `knowledge/DECISIONS.md` سجلّ القرارات (الأحدث في الرأس) — اقرأ رأسه قبل أيّ تعديل؛
  `knowledge/PITFALLS.md` فخاخ مثبتة تجريبيًّا؛ `CONTRIBUTING.md` قواعد اللمس.
  ملاحظة: `knowledge/PROJECT.md` أقدم من الواقع في نقطة واحدة — مشروع Vercel **مربوط الآن بـGitHub**:
  دمج `main` ينشر تلقائيًّا، وكلّ طلب سحب يحصل على معاينة.

## الأوامر
```
npm run ci        # check + guard + coldstart + verify + test — يجب أن يمرّ كاملًا قبل أيّ التزام
npm run bundle    # بعد أيّ تعديل على js/app-NN-*.js أو js/partials-*.js
node tests/x.test.cjs   # اختبار واحد
```

## القواعد التي يفحصها `npm run ci` (وتفشل لو خالفتها)
- لا `catch (e) {}` فارغ: إمّا `__swallow(e, "…")` في العميل، أو تعليق يشرح، أو `/* guard-ok — … */`.
- لا سرّ ولا مفتاح في ملفّات العميل، ولا قراءة `SITE_URL` خامًا.
- لا تصادم أسماء `const` في نطاق أعلى الحزمة (الأجزاء تُلصق في ملفّ واحد).
- نقاط الدخول `api/*.js` تُحمَّل في بيئة عارية بلا أيّ متغيّر: لا تقرأ الأسرار في نطاق الوحدة، بل داخل المعالج.
- الحزمة مطابقة لأجزائها (`verify`).

## قواعد العمل
1. افهم قبل أن تغيّر: اقرأ الملفّ كاملًا، ورأس `DECISIONS.md`، وابحث عن السلوك في الاختبارات القائمة.
2. لكلّ سلوك جديد اختبار في `tests/` مسجّل في `package.json`، ومدخل موجز في رأس `DECISIONS.md`
   بصيغة: **العرض** (ماذا رأى المالك) → **القرار** (ماذا فعلنا ولماذا) → **الاختبار**.
3. رسالة الالتزام عربيّة: السطر الأوّل ما تغيّر ولماذا، ثمّ التفاصيل.
4. أصغر تغيير يحلّ المشكلة. لا ميزة ولا إعادة هيكلة خارج المهمّة. لا تعطيل اختبار ولا تخطّيه.
5. سطح المكتب أوّلًا؛ لا تلمس تخطيط الجوّال بلا طلب. لا تحذف `stockTickerToggle`، و«مها» تبقى في مكانها،
   والوسائط الكبيرة غير المستخدمة تبقى (قرار مالك).
6. **الأسرار:** لا تطبع مفتاحًا ولا تكتبه في ملفّ ولا في تعليق. أشكالها المعروفة: `sk-ant-api03-` مفتاح
   Anthropic، `sk-or-` مفتاح OpenRouter (مكانه `OPENROUTER_API_KEY` لا `ANTHROPIC_API_KEY`)،
   `github_pat_`/`ghp_` رموز GitHub. المالك يحفظ أسراره في Vercel أو أسرار المستودع أو خزنة التطبيق.

## داخل GitHub Actions (omran-agent.yml)
- تصلك المهمّة من وكيل التطبيق عبر مسألة GitHub، ومعها `AGENT_CONTEXT.md` (آخر الالتزامات، المسائل،
  الطلبات، فحوص CI، نشرات Vercel وسجلّ البناء) — اقرأه أوّلًا. لا يُلتزم هو ولا `AGENT_SUMMARY.md`.
- عندك `gh` برمز GitHub للقراءة والتعليق على مسألة المهمّة، وChromium + Playwright لمسابر
  `scripts/*-probe.mjs` على الإنتاج، وWebSearch/WebFetch للتوثيق.
- التزم على الفرع المحدّد فقط. **لا تدفع، لا تفتح طلب سحب، لا تدمج، لا تلمس `.github` ولا الأسرار.**
  الورك فلو يدفع ويفتح الطلب، والمالك يراجع ويدمج، وVercel ينشر.
- اكتب ملخّصك في `AGENT_SUMMARY.md` بالعربيّة: ماذا غيّرت ولماذا وكيف تحقّقت وما تركته وسببه.

## طريقة العمل التي يتوقّعها المالك (منقولة من جلسات الويب — تُطبَّق حرفيًّا في الجسر أيضًا)
1. **افهم أوّلًا:** اقرأ رأس `knowledge/DECISIONS.md` و`knowledge/PITFALLS.md`، والملفّ المعنيّ كاملًا لا مقطعًا، والاختبارات القائمة للسلوك. لا تخمّن ما يمكن قراءته.
2. **أعد إنتاج المشكلة قبل إصلاحها:** لقطة (`scripts/ui-shot.mjs`)، أو مسبار، أو اختبار يفشل. ما لم تره لا تصلحه، وما أصلحته أثبته بالشيء نفسه بعد الإصلاح.
3. **أصغر تغيير يحلّ الجذر:** لا ميزة ولا إعادة هيكلة خارج المهمّة، ولا تعطيل اختبار ولا تخطّيه. إن وجدت خطأً آخر في الطريق فاذكره ولا تصلحه إلّا بطلب.
4. **لكلّ سلوك جديد:** اختبار في `tests/` مسجّل في `package.json`، ومدخل في رأس `DECISIONS.md` (**العرض** → **القرار** → **الاختبار** والدرس)، وفخّ جديد في `PITFALLS.md` إن وقعت فيه.
5. **التحقّق قبل الإبلاغ:** `npm run bundle` إن لمست الأجزاء، ثمّ `npm run ci` كاملًا حتّى يمرّ، ولقطة حاسوب + جوّال بـ`scripts/ui-shot.mjs` لأيّ تغيير مرئيّ (مهارة `verify-ui`). لا تقل «تمّ» قبل أن ترى.
6. **الالتزام والنشر:** رسالة عربيّة (السطر الأوّل ما تغيّر ولماذا). في الجسر: التزم على فرع غير `main`؛ وإن طلب المالك «انشر» في المهمّة فادفع الفرع وافتح طلب السحب بـ`gh pr create` وأعطه الرابط؛ الدمج بأمره «ادمج» وحده.
7. **التقرير للمالك:** ماذا تغيّر، كيف تحقّقت (بالأرقام واللقطات)، ما تركته وسببه، رابط الطلب. بالعربيّة، مباشرًا، وبلا مقدّمات؛ وإن كان التغيير كبيرًا فاشرح ما ستفعله قبل أن تفعله ثمّ نفّذه كاملًا لا نصفًا.

## قواعد المالك الثابتة (لا تناقَش)
- **الأبواب المقفلة:** المال (Stripe/PayPal/الأرصدة/النقاط/الباقات)، بيانات المستخدمين، حذف مستودعات أو قواعد بيانات، أيّ إنفاق، نشر المفاتيح — لا تُمسّ إلّا بموافقة صريحة على البند نفسه.
- **لا اسم مزوّد أو نموذج** (كلاود، جيمناي، غروق، GPT…) في أيّ نصّ يراه المستخدم داخل التطبيق: الأسماء الوظيفيّة فقط. «مها» اسم المساعد الصوتيّ وتبقى؛ `stockTickerToggle` يبقى؛ الوسائط الكبيرة غير المستخدمة تبقى.
- **سطح المكتب أوّلًا؛ الجوّال يُلمس عند الطلب فقط** ولا ينكسر. **١٤ لغة:** أيّ نصّ واجهة جديد يُضاف لكلّ اللغات (`js/app-03-i18n-data.js` + `i18n/*.js`) وترفع وسم `?v=` للملفّات المحمّلة منفصلة.
- **البنية الحيّة:** Vercel ينشر `main` تلقائيًّا ويعطي معاينة لكلّ طلب سحب؛ جسر Claude Code (`cc-bridge/`) على Railway ويعيد نشر نفسه عند تغيّر `cc-bridge/**`؛ المفاتيح في Vercel وRailway وأسرار المستودع، لا في الملفّات.

## أدوات التحقّق الجاهزة
- `node scripts/ui-shot.mjs --out /tmp/shots --user omran --both [--settings accountSection|home] [--eval "JS"] [--full]` — لقطات حاسوب وجوّال محلّيًّا بلا خادم ولا مفاتيح، مع أخطاء الصفحة (يفشل إن وُجدت).
- `scripts/*-probe.mjs` — مسابر على الإنتاج (تحتاج شبكة)، و`scripts/tv-check.mjs` للقنوات.
- `node tests/x.test.cjs` — اختبار واحد؛ `npm run ci` — الكلّ (check + guard + coldstart + verify + test).
