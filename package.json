{
  "name": "omran-ai-builder",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@vercel/blob": "^0.27.1",
    "web-push": "^3.6.7"
  },
  "scripts": {
    "bundle": "node scripts/build.mjs",
    "verify": "node scripts/build.mjs && git diff --exit-code js/app.bundle.js index.html sw.js",
    "check": "for f in js/app-*.js sw.js api/*.js api/_lib/*.js; do node --check \"$f\" || exit 1; done"
  }
}
