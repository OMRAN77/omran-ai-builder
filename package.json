{
  "name": "omran-ai-builder",
  "version": "1.0.0",
  "engines": {
    "node": "24.x"
  },
  "private": true,
  "dependencies": {
    "@vercel/blob": "^0.27.1",
    "web-push": "^3.6.7"
  },
  "scripts": {
    "bundle": "node scripts/build.mjs",
    "verify": "node scripts/verify-bundle.mjs",
    "check": "for f in js/app-*.js sw.js api/*.js api/_lib/*.js; do node --check \"$f\" || exit 1; done",
    "guard": "node scripts/guard.mjs",
    "test": "node tests/owner-gate.test.cjs && node tests/memory-profile.test.cjs && node tests/chat-style.test.cjs && node tests/removed-search-source.test.cjs",
    "ci": "npm run check && npm run guard && npm run verify && npm test",
    "smoke": "node scripts/smoke.mjs"
  }
}
